<h1><%= @employee.first_name.to_s + ' ' + @employee.last_name.to_s %></h1>

<p>Office: <%= @employee.office %></p>

<p>Title: <%= @employee.title %></p>

<p>Alias: <%= @employee.alias %></p>

<p>Dog: <%= @employee.dog.name %></p>

<p>Picture:
    <% if @employee.img_url.present? %>
        <div><%= image_tag "#{@employee.img_url}" %></div>
    <% else %>
            No Picture Available
    <% end %>
</p>